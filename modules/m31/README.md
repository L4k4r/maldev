# Payload Staging - Windows Registry

This executable retrieves an encrypted payload from the internet. It puts this encrypted payload in a registry. Next, it retrieves the encrypted payload from the registry, decrypts it and executes it.

![""](images/payloadStagingWinReg.png")
